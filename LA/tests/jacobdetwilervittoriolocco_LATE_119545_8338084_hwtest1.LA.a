void main() {
:entrance
h()
e()
l()
l()
o()
space() 
w()
o()
r()
l()
d()
return
}

void h(){
    :entrance
    int64 var
    var <- 209 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void e(){
    :entrance
    int64 var
    var <- 203 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void l(){
    :entrance
    int64 var
    var <- 217 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void o() {
    :entrance
    int64 var
    var <- 223 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void w(){
    :entrance
    int64 var
    var <- 239 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void space(){
    :entrance
    int64 var
    var <- 65 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void r(){
    :entrance
    int64 var
    var <- 229 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}

void d(){
    :entrance
    int64 var
    var <- 201 //pass in first argument (h in ASCII) according to calling convention
    print(var)
    return
}
